import React from 'react';

const MenuButton = () => {
  return (
    <button className="text-gray-700 text-sm uppercase tracking-wider font-medium">
      Menu
    </button>
  );
};

export default MenuButton;